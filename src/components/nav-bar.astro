---
const links: { name: string, path: string }[] = [
  {
    name: 'Home',
    path: '#hero'
  },
  {
    name: 'Projects',
    path: '#projects'
  }
]
---
<nav id="nav-bar" class="w-full h-20 fixed top-0 px-28 z-40">
  <section class="w-3"></section>
  <ul class="flex grow w-full h-full justify-end gap-2 items-center">
    {
      links && links.map(link => 
        <li class="font-mono">
          <a href={link.path}>{link.name}</a>
        </li>
      )
    }
  </ul>
</nav>
<script>
  import '../scripts/nav-bar';
</script>